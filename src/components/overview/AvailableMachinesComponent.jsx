import React from 'react';
import { Card, CardBody, CardHeader } from 'reactstrap';

export default class AvailableMachinesComponent extends React.Component {
    render () {
        return (
            <Card>
                <CardHeader>Available Machines</CardHeader>
                <CardBody>
                    test
                </CardBody>
            </Card>
        );
    }
}